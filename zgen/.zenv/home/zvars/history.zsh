# Just let me have an infinite history!
export HISTSIZE=9999999999999999

# Finally setting on .zhistory to be my history file
# because work is setup that way and I don't want to merge them.
# Ugh, why can't there be one?
export HISTFILE="${HOME}/.zhistory"
